import React from 'react';
import SearchTeacher from './Search';

const Staff = ({ match }) => (
  <div className="main-content courses">
    <div className="course-header group">.
      <h2>Gililland Staff</h2>
    </div>
    <SearchTeacher />
  </div>
);

export default Staff;
